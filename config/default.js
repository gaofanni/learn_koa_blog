const config = {
    port: 4000,
    database: {
        DATABASE: 'nodesql',
        USERNAME: 'root',
        PASSWORD: 'root',
        PORT: '4040',
        HOST: 'localhost'
    }
}
module.exports = config